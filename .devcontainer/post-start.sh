#!/usr/bin/env bash
# Post-start script for aieng devcontainer
# Runs every time the container starts

echo "[aieng-devcontainer] aieng workspace is ready."
echo "  Run 'make dev' to start both backend and frontend."
echo "  Or start individually: make backend / make frontend"
