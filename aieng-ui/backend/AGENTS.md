# aieng Workbench — Agent Guide

The canonical agent guide lives at the **workspace root**: [`../../AGENTS.md`](../../AGENTS.md).

At runtime, call the MCP tool **`aieng.agent_readme`** to receive it in-band.

This file is intentionally a pointer so the content never drifts across copies.
For MCP wiring (Claude Code / Copilot / Codex) see [MCP_SETUP.md](MCP_SETUP.md).
