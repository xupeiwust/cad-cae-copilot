{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://aieng.dev/schemas/design_candidate_patch.schema.json",
  "title": "aieng Design Candidate Patch v0",
  "description": "A proposed set of design-variable changes for a design study. v0 candidates are VALIDATED and NORMALIZED but NEVER applied — no geometry recompile, no CAE, no baseline mutation.",
  "type": "object",
  "additionalProperties": true,
  "required": ["format", "candidate_id", "variable_changes"],
  "properties": {
    "format": {"const": "aieng.design_candidate_patch"},
    "schema_version": {"type": "string"},
    "candidate_id": {"type": "string"},
    "reasoning": {"type": "string", "description": "Why these changes (required when the study sets require_reasoning)."},
    "selected_part_id": {"type": "string", "description": "Assembly-aware scope; changes must stay within this part."},
    "variable_changes": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["variable_id", "new_value"],
        "properties": {
          "variable_id": {"type": "string"},
          "new_value": {}
        }
      }
    },
    "provenance": {"type": "object", "additionalProperties": true}
  }
}
