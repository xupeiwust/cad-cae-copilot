# Reference context for the mock bracket demo.
# These feature IDs are generated by the current MockTopologyExtractor + rule-based recognizer.
material: Al6061-T6

protected_features:
  - feat_hole_pattern_001

simulation:
  type: static_structural
  fixed:
    - feat_hole_pattern_001
  loads:
    - target: feat_base_plate_001
      type: force
      value_n: 500
      direction: [1, 0, 0]

targets:
  max_von_mises_stress_mpa: 120

assumptions:
  - Mounting hole pattern is treated as fixed support.
  - Load target is provided by user context.
