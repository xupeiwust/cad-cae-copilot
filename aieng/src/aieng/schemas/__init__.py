"""Packaged JSON Schemas for `.aieng` resources.

This package exists so that schema files ship with installed wheels and can
be loaded via :func:`importlib.resources.files`. Do not import schemas
directly; use the loader helpers in :mod:`aieng.validate`.
"""
